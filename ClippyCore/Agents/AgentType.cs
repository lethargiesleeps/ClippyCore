using System;
using System.Collections.Generic;
using System.Text;

namespace ClippyCore.Agents
{
    /// <summary>
    /// Enum with values for each agent. When adding a new agent to make available, add an identifier here.
    /// </summary>
    public enum AgentType
    {
        Clippy,
        Bonzi,
        Shrek
    }
}
